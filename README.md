# PDF RAG

An experimental RAG project focused on reproducible evaluation with QASPER.

The evaluation code lives in `src/evaluation`, experiment configurations in
`configs/qasper`, and generated results in `artifacts/qasper`.
